1. tworzenie pliku szyfrujacego (enter bez hasla = nie pyta nigdy o nic)
```bash
ssh-keygen -t ed25519 -C "nazwa"
```
2. zapamietanie hasla
```bash
ssh-copy-id user@ip
```
podaj haslo aktualne ostatni raz.


ewentuane usuwanie tego
```bash
rm ~/.ssh/id_ed25519 ~/.ssh/id_ed25519.pub
```
